# Public downloads

Free assets served directly at `/downloads/<file>`. Anything **paid** belongs in
the private Supabase Storage `products` bucket instead — see
[`docs/GROWTH-ARCHITECTURE.md`](../../docs/GROWTH-ARCHITECTURE.md).

## Required

| File | Used by |
| ---- | ------- |
| `nlh-to-plo-survival-card.pdf` | The welcome email sent by `/api/subscribe` |

**This file is not here yet.** Until it is added, the Reality Check's email
exchange sends a link that 404s. Export it from
`FREE_NLH_PLO_Survival_Card_FINAL.docx` in the publication set.

No placeholder is committed in its place: a broken link is easier to notice than
a file that looks real and is not.
